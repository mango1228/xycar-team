<launch>

  <!-- USB 카메라 (hough_drive.launch와 동일 파라미터) -->
  <node name="usb_cam" output="screen" pkg="usb_cam" type="usb_cam_node">
    <param name="video_device" value="/dev/videoCAM" />
    <param name="autoexposure" value="false" />
    <param name="exposure" value="162" />   <!-- 영상이 너무 어둡/밝으면 이 값을 바꿔봐 -->
    <param name="image_width" value="640" />
    <param name="image_height" value="480" />
    <param name="pixel_format" value="yuyv" />
    <param name="camera_frame_id" value="usb_cam" />
    <param name="io_method" value="mmap" />
  </node>

  <!-- Canny/ROI 검증 노드 (모터 없음 - 차 안 움직임) -->
  <node name="canny_tune" output="screen" pkg="my_motor" type="canny_tune.py" />

</launch>
